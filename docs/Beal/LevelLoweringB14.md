# v8.78.0 B≡14 list and displayed 2-torsion

`lean/BealLevel26Foundations/Beal/FullProof/LevelLoweringB14.lean`
`lean/BealLevel26Foundations/Beal/FullProof/Step60B14List.lean`

`step60_b14_list` is 352 named Step60
numerals, all `B % 16 = 14`.
`b14_witnesses` is the matching
`(B, 53, a53)` list.

`frey_modular_B14` is the displayed cubic
identity `x(x-B^4)(x+(B+3)^4) = 0` at
`0`, `B^4`, `-(B+3)^4`.  Axiom `propext`.
That is **not** Wiles, **not** BCDT, **not**
the existing `frey_modular` (still
`Classical.em`).

`level_lowering_to_26_B14` stays a Prop.
The recorded pack is a miss, not a
congruence, and not Ribet.

`beal_4_13_13_gap3_B_le_2M_eliminated_B14_honest`
stays the uninhabited forall on the 352
rows.  A trace miss is not `¬ ∃ A`.

`beal_4_13_13_gap3_B_le_2M_eliminated_full_honest`
is the displayed two-element list via
mod 16.  `#print axioms` empty.

Chain `ExistsNewformLevel2` stays `0 != 0`.
Does not import `RibetMazur`.
Generated by `scripts/gen_b14_kraus.py --dump-b14 --format lean`.
