# v8.67.0-level26-newforms-skeleton Level 26 newforms

`lean/BealLevel26Foundations/Beal/FullProof/Level26_Newforms.lean`
and `RibetMazur.lean`

Displayed prefixes of the locked
`CoefficientLedger_26` arrays for `26a1`
and `26b1` (LMFDB labels `26.2.a.a` /
`26.2.a.b` are displayed names, not a
Mathlib identification).  The first 20
coefficients match `qExp_26a1` /
`qExp_26b1`.  Ledger lock: `a₃(26a1)=1`,
`a₅(26a1)=-3`, `a₃(26b1)=-3`,
`a₅(26b1)=-1`.  Displayed dim
`S₂(Γ₀(26))=2` is not a Mathlib
cusp-form dimension theorem.

`kraus_primes_26` is the Track B list
`[53, 79, 131, 157, 313, 443, 521, 547]`.
`zsig_density_links_to_kraus` is that
list equality plus
`4488 + 5 * 299 = 5983`.  That does
**not** inhabit Kraus matching.

`kraus_condition` is a displayed
`ZMod 13` equality of two `Int`s, not a
Frey=newform theorem.

Does **not** import `RibetMazur` (cycle).
Does **not** import
`Mathlib.NumberTheory.ModularForms.Basic`.
Not imported by the 24-module none chain.

`level26_a_eliminated_by_53`,
`level26_b_eliminated_by_443`, and
`kraus_elimination_q_13_level_26_proof_sketch`
stay uninhabited.  A p=53 or p=443 Φ₁₃
hit is not Kraus matching.
`kraus_elimination_q_13_level_26` stays
uninhabited.
`ribet_26_to_2_from_displayed_miss`
stays uninhabited.
`ExistsNewformLevel2` stays `0 != 0`.
`beal_from_ribet` still takes
`ModularImpliesLevel2Newform`.
No new Beal forall.
Not Full Mathlib forall.
