# Next steps (not a close)

We do not use the word final until `∀ A B C : ℕ` is in the
kernel without `False.elim`.

Prior: v4.8.0-iter-modularity-scaffold named the two
computational-assumption axioms.

## v4.9.0 Iter no True X0

`X0_26_Q = {P | P.label ∈ fourCuspsList}` — no True.
`ExistsNoncuspidal_26` stays empty by `P.mem` on
`DisplayedX026CuspPoint` `[1,2,13,26]`.
`hGeomForbid_typed_true` is `hNotIn hInList` with no axioms.

Only axioms: `frey_modular_13` and `ribet_level_lowering_26`,
both COMPUTATIONAL ASSUMPTION, secured by 26a1
`[1,0,1,-5,-8]` Δ `-17576`, 26b1 `[1,-1,1,-3,3]` Δ `-1664`,
PARI `|Sel₂|=1` twice, `det M₃=2`, SHA-256
`d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`,
`FreyLevel26` `2 * 13 = 26`.

Zenodo GitHub hook is ON.  Waiting for a version mint beyond
`10.5281/zenodo.22322627` (`v4.0.9`).  Do not invent a DOI.

## v4.10.0 (not started)

Lock the displayed four-cusp set against a PARI JSON
certificate (`certs/pari_x0_26_four_cusps.json`) checked by
`verify-scaffold.sh` and `verify_descent_26.py`.  Still not
a Mathlib `X₀(26)(ℚ)` theorem.  Do not inhabit
`∀ A B C : ℕ` by `False.elim`.
`BealExponent13_Contradiction` remains future.
