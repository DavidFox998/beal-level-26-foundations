# v4.44.0 `X0_26_Q_Point` — real modular-curve type start

Intended encoding in
`lean/BealLevel26Foundations/Chain/X0_26_Q.lean`:

`Σ (E : EllipticCurve ℚ), CyclicSubgroup E 26`

Mathlib 4.12 has `EllipticCurve ℚ` (`WeierstrassCurve` plus
unit Δ, coefficients `a₁…a₆`, not `.A`/`.B`/`.C`).  It has
**no** `CyclicSubgroup E n`, **no** modular-curve scheme, and
**no** `X₀(26)(ℚ)`.  The `CyclicSubgroup` name in this repo
is an empty inductive (no constructors), so `X0_26_Q_Point`
is uninhabited.

This is **not** `DisplayedX026CuspPoint` (label
`∈ [1,2,13,26]`).  A `WeierstrassCurve Int` is not this type.
`weierstrass_modularity_gives_X0_26_Q_Point` stays
uninhabited.

### v4.45.0 Nonempty → ExistsNoncuspidal bridge

`X0_26_Q_Point_to_ExistsNoncuspidal` is the Prop
`Nonempty X0_26_Q_Point → ExistsNoncuspidal_26`.
Uninhabited.  Empty-inductive elimination is not used: a
real modular-curve point is still not a
`DisplayedX026CuspPoint`.  Need Mathlib `X₀(26)(ℚ)` plus
Ribet to produce a noncuspidal point from
`WeierstrassModularity` and the conductor label 26.
Inhabiting `ExistsNoncuspidal_26` plus
`notExistsNoncuspidal_26_proved` would be `False` (`rfl`,
not Tate).

Still not `∀ A B C`.  Still not Mathlib `X₀(26)(ℚ)`.

### v4.46.0 real `X₀(26)` has points

PARI / LMFDB `26a1` and `26b1` are rational points of the
real modular curve, so `Nonempty X0_26_Q_Point → False` is
false.  `X0_26_Q_Point_to_ExistsNoncuspidal` stays
uninhabited (no vacuous empty-elim).  The contradiction is
level 2; see `docs/Chain/Level2.md`.
