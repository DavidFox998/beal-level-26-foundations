# About — Beal Level 26 Foundations

Latest tag / HEAD: `v4.55.0-iter-delta-ne0-explicit-conditional-propext-only`.

Latest parallel mint remains
[10.5281/zenodo.22549784](https://doi.org/10.5281/zenodo.22549784)
(v4.54.0) until a new mint.  Hook
[10.5281/zenodo.22379293](https://doi.org/10.5281/zenodo.22379293).
`IsVersionOf` [10.5281/zenodo.22272382](https://doi.org/10.5281/zenodo.22272382)
is metadata only.  Original-family latest remains
[10.5281/zenodo.22322627](https://doi.org/10.5281/zenodo.22322627).
Chain `… → 22549784`.

v4.55.0 makes Δ ≠ 0 explicit from
`FreyCurve13_of_BealCounterexampleBases`
`Y² = X(X − A¹³)(X + B¹³)` via `frey_Delta13_ne_0_of_pos`
(`0 < A`, `0 < B`).  Conditional Beal `∀` now has a
**propext-only** variant that takes Weierstrass modularity
as a hypothesis (`hTate → hRibet → hWeierstrass → Beal ∀`).
`Is13Case → False` stays a valid type, uninhabited.
Real `X₀(26)(ℚ)` has `26a1` / `26b1`.

Still not `∀ A B C` in the kernel unconditionally.  Still
not Mathlib `X₀(26)(ℚ)` + Ribet + Tate but conditional
Beal `∀` is propext only via explicit Δ ≠ 0 and
`Is13Case → False` as the only honest path.
8 modules green.  `NO_SORRY_OK`.

This is the in-repo About lock for GitHub About / topics /
homepage wording.  README badges stay on `22452680`.
