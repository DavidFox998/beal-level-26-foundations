# v4.15.0 Iter Typed Forall Bridge

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Bridges `BealTheorem_Exponent13_Typed` into the Forall
file without new axioms:

* `def beal_forall_from_ribet := beal_exponent13_from_ribet`
* `-- BRIDGE: Uses certified_from_ribet which is none via
  hGeomForbid, upstream certified needs both axioms`
* `certified_from_ribet` / `beal_exponent13_from_ribet` still
  depend on no axioms (`hGeomForbid_typed_true` via
  `hNotIn hInList`)
* `contradiction_from_ribet` still uses
  `ribet_level_lowering_26` only
* `BealExponent13_Contradiction.certified` still uses
  `frey_modular_13` and `ribet_level_lowering_26`
* Forall file still has no `True`, no `sorry`, no
  `False.elim`
* `verify-scaffold.sh` greps `beal_forall_from_ribet`

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
No `sorry`. No `False.elim`.
