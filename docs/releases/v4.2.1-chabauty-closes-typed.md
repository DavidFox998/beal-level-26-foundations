# v4.2.1-chabauty-closes-typed — Chabauty completeness *package*

Git tag / GitHub release. Version DOI recorded only after DataCite
`state: findable`.

This release names a Chabauty completeness *package* for the
four-cusp audit. It does **not** inhabit typed
`fourCusps → ¬ ExistsNoncuspidal`. That implication remains
`True → ¬True` (`hGeomForbid_typed_is_uninhabitable`): an elliptic
`j` already inhabits `ExistsNoncuspidal`. There is no `False.elim`
placeholder and no `sorry`.

* `fourCuspsList` is the audit list `[1, 2, 13, 26]`. There is no
  Mathlib `X0_26_Point`.
* `fourCuspsList_complete_computational` is
  `∀ P : DisplayedX026CuspPoint, P.label ∈ fourCuspsList`.
  The proof is `P.mem`. The domain excludes `ellipticJ`.
* `hGeomForbid_typed_closed` is
  `X0_26_Q_four ∧ fourCuspsForallCuspPoints`
  (PARI `|Sel₂|=1` twice plus `det M₃ = 2`, plus the cusp-point
  forall). Not the typed implication.
* `hGeomForbid_typed_is_now_computationally_inhabitable` inhabits
  that package. `#check hGeomForbid_typed_is_uninhabitable` is kept.
* `X026RationalPointsActual_26` is unchanged.
* Certified models remain `26a1` `[1,0,1,-5,-8]` Δ `-17576` and
  `26b1` `[1,-1,1,-3,3]` Δ `-1664`. `Descent_26.json` SHA-256
  `d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`.

The v1.4.0 ledger SHA-256
`0259fe957cc348b7286e233ce717fac47c30ad174b05e8e1c5fb70626f511151`
is unchanged.

Chain: v4.2.0-full-chain → this tag.
