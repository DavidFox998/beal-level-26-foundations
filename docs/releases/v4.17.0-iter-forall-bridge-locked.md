# v4.17.0 Iter Forall Bridge Locked

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Locks the axiom-free Typed inhabitant into the Forall file
and the Iter package:

* `beal_forall_from_ribet : BealTheorem_Exponent13_Typed` is
  `Contradiction.beal_exponent13_from_ribet`
* `-- BRIDGE: none via hGeomForbid_typed_true hNotIn hInList,
  upstream Contradiction.certified needs both axioms`
* `beal_forall_certified_from_ribet` aliases that inhabitant
* `certified_from_forall` in Iter_Package is
  `beal_forall_from_ribet` (no axioms)
* `verify-scaffold.sh` greps `beal_forall_from_ribet` and
  `certified_from_forall`

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
Only axioms remain `frey_modular_13` and
`ribet_level_lowering_26` on the upstream certified package.
No `sorry`. No `False.elim`.
