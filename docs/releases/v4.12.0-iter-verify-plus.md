# v4.12.0 Iter Verify Plus

GitHub release. Version DOI recorded only after DataCite
`state: findable`. Latest minted Zenodo version remains
`v4.0.9-fourCusps-forall`
([10.5281/zenodo.22322627](https://doi.org/10.5281/zenodo.22322627)).
Zenodo GitHub hook is ON; waiting for a mint beyond that DOI.

Adds verify-plus checks in `scripts/verify-scaffold.sh` for
the v4.10.0 PARI lock and the renamed 2-descent figure:

* `docs/images/2-descent-level-26-26a1-26b1.webp` exists and
  is at least 300 KB
* Facebook upload name
  `797726399_1063690106418837_5544926896290683567_n.webp`
  is gone
* README and `docs/README.md` carry
  `PARI 2-Descent Certificates — X0(26) Four Cusps` and
  the renamed image
* `X0_26_Point.lean` cites that image
* `certs/pari_x0_26_four_cusps.json` keys are `26a1` /
  `26b1` / `SHA` (not `curve_*` / `SHA_cert`)
* `|Sel₂|=1` twice, `rank` `0`, `M3_det` `2`,
  `fourCusps` `[1,2,13,26]`, Frey `2*13=26`
* SHA
  `d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`

`X0_26_Q = {P | P.label ∈ fourCuspsList}` (no `True`).
`ExistsNoncuspidal_26` empty by `P.mem`.
`hGeomForbid_typed_true` no axioms.

Only axioms: `frey_modular_13` and `ribet_level_lowering_26`,
both COMPUTATIONAL ASSUMPTION. Not a Mathlib `X₀(26)(ℚ)`
theorem. Not `∀ ℕ`. No `sorry`. No `False.elim`.
v4.11.0 `BealExponent13_Contradiction` is still not started.
