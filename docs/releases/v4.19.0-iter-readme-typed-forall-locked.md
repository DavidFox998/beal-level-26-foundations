# v4.19.0 Iter Readme Typed Forall Locked

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Locks the README / docs theorem table to commit `c9ab61b`
(`v4.18.0-iter-typed-forall-package-locked`):

* `typed_and_forall` none
  `⟨beal_exponent13_from_ribet, beal_forall_from_ribet⟩`
* `certified_typed_and_forall` none
  `⟨certified_from_forall, beal_forall_certified_from_ribet⟩`
* PARI figure
  `docs/images/2-descent-level-26-26a1-26b1.webp` unchanged
* About text records the Typed ∧ Forall none lock

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
No `sorry`. No `False.elim`.
