# v4.0.13-formal-immersion-proof-no-doi — named `det ≠ 0` immersion package

Git tag only. No Zenodo deposit and no version DOI.

Phase 3 Track B. Mathlib 4.12 has no completed-local-ring formal
immersion and no residue-disk API. This release does **not** add
`sorry`.

* `Jacobian/FormalImmersionActual_26.lean` — keeps
  `FormalImmersionAtTwo26.of_qExpansion` with `input = M₃`,
  `input_eq`, `det = 2 ≠ 0`, and `M₃² = I`. Adds
  `formalImmersion_at_2_surjective_from_M3` as
  `of_qExpansion.input.det ≠ 0`, certified by `decide` after
  rewriting `input_det`. Blueprint citation: Mazur 1978
  Prop III.3.2. The Lean `Prop` is that determinant check. It is
  not surjectivity of a map of completed local rings.
* `Jacobian/ResidueDisksProof_26.lean` —
  `diskEqualityFromImmersion` aliases that same `det ≠ 0`. Not a
  residue-disk theorem.

`X026RationalPointsActual_26` is unchanged. Typed `hGeomForbid`
remains uninhabitable. Tracks C and D are not started. There is
no `theorem BealTheorem`.

Chain: v4.0.12-scheme-stub-no-doi → this tag.

The v1.4.0 ledger SHA-256
`0259fe957cc348b7286e233ce717fac47c30ad174b05e8e1c5fb70626f511151`
is unchanged.

Version DOI: none (tag only).

Concept DOI: [10.5281/zenodo.22272382](https://doi.org/10.5281/zenodo.22272382)
