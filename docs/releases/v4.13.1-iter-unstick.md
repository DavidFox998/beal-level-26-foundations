# v4.13.1 Iter Unstick

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Keeps the v4.13.0 contradiction-start wiring and cleans
`verify-scaffold.sh` so it locks certificates and images,
not DOI record ids:

* `docs/images/2-descent-level-26-26a1-26b1.webp` exists and
  is at least 300 KB
* Facebook upload name is gone (`test ! -f`)
* README has `PARI 2-Descent Certificates` and the renamed
  image
* `certs/pari_x0_26_four_cusps.json` keys `26a1` / `26b1` /
  `SHA` `d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`
* `X0_26_Point.lean` has `fourCuspsList` `[1,2,13,26]`,
  `X0_26_Q_eq_fourCuspsList` `rfl`, and no `∨ True`
* `Mazur/BealExponent13_Contradiction.lean` has
  `contradiction_from_ribet` and `beal_exponent13_from_ribet`

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
Only axioms: `frey_modular_13` and `ribet_level_lowering_26`.
No `sorry`. No `False.elim`.
