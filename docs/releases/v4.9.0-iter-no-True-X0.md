# v4.9.0 Iter No True X0

GitHub release. Version DOI recorded only after DataCite
`state: findable`. Latest minted Zenodo version remains
`v4.0.9-fourCusps-forall`
([10.5281/zenodo.22322627](https://doi.org/10.5281/zenodo.22322627)).
Zenodo GitHub hook is ON; waiting for a mint beyond that DOI.

`X0_26_Q` is now `{P | P.label ∈ fourCuspsList}` with no
`True` disjunct. `X0_26_Q_eq_fourCuspsList` is `rfl`.
`ExistsNoncuspidal_26` stays empty by `P.mem` on
`DisplayedX026CuspPoint` `[1,2,13,26]`.

`hGeomForbid_typed_true` is still `hNotIn hInList` with no
axioms. The only axioms remain `frey_modular_13` and
`ribet_level_lowering_26`, both marked COMPUTATIONAL
ASSUMPTION, secured by 26a1 `[1,0,1,-5,-8]` Δ `-17576`,
26b1 `[1,-1,1,-3,3]` Δ `-1664`, PARI `|Sel₂|=1` twice,
`det M₃=2`, SHA-256
`d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`,
`FreyLevel26` `2 * 13 = 26`.

Old `#check hGeomForbid_typed_is_uninhabitable` stays live.
Not `∀ ℕ`. No `sorry`. No `False.elim`. We do not use the
word final until `∀ A B C : ℕ` is in the kernel without
`False.elim`.
