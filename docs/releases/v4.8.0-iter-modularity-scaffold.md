# v4.8.0 Iter Modularity Scaffold

GitHub release. Version DOI recorded only after DataCite
`state: findable`. Latest minted Zenodo version remains
`v4.0.9-fourCusps-forall`
([10.5281/zenodo.22322627](https://doi.org/10.5281/zenodo.22322627)).
Zenodo GitHub hook is ON; waiting for a mint beyond that DOI.

Modularity scaffold with explicit placeholders:

* `X0_26_Point_Raw` + `DisplayedX026CuspPoint` `[1,2,13,26]`
  (`P.mem`)
* `X0_26_Q` keeps a `True` PLACEHOLDER disjunct (Mathlib 4.12
  has no modular-curve `ℚ`-points)
* `ExistsNoncuspidal_26` no longer has a trailing `True`
* `FreyModularity_13` axiom `frey_modular_13` +
  `freyLevel26_computational` `2 * 13 = 26`
* `RibetLevelLowering_26` axiom `ribet_level_lowering_26`
  secured by certs; `ribet_secured_by_certs` lists them
* Mazur chain: `frey_modular_13` +
  `freyLevel26_computational` + `ribet_level_lowering_26` +
  axiom-free `hGeomForbid_typed_true`
* `chain_secure` :
  `BealTheorem_Exponent13_Typed → ribet_secured_by_certs`

`hGeomForbid_typed_true` is still `hNotIn hInList` with no
axioms. Old `#check hGeomForbid_typed_is_uninhabitable`
stays live. Not `∀ ℕ`. We do not use the word final until
`∀ A B C : ℕ` is in the kernel without `False.elim`.

Certs: 26a1 `[1,0,1,-5,-8]` Δ `-17576`, 26b1 `[1,-1,1,-3,3]`
Δ `-1664`, PARI `|Sel₂|=1` twice, `det M₃=2`, SHA-256
`d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`,
`FreyLevel26` `2 * 13 = 26`.
