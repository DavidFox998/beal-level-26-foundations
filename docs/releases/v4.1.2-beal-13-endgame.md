# v4.1.2-beal-13-endgame — computational `hGeomForbid` from PARI `|Sel₂|=1`

Git tag only. No Zenodo deposit and no version DOI.

`Mazur/EndgameScaffold` now records a *computational* `hGeomForbid`
boundary from the v4.1.1 PARI archive. This release does **not** add
`sorry` and does **not** inhabit typed `hGeomForbid`
(`fourCusps → ¬ ExistsNoncuspidal`). There is no `theorem BealTheorem`.

* `rankZero_unconditional` is
  `RankZero_26a1_from_Selmer ∧ RankZero_26b1_from_Selmer`, i.e.
  `SelmerBound = 1` twice (`1 = 1`), certified from
  `SelmerBound_*_certified`.
* Computational evidence is PARI `ellrank` + `ell2cover` + `elltors`
  in `Certs/Descent_26.json` (SHA-256
  `d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`).
  That is not a Lean Mordell--Weil theorem. Mathlib 4.12 has no
  `SelmerGroup`.
* `hGeomForbid` is `Option.some` of
  `rankZero_unconditional` plus the same-shape `det M₃ ≠ 0`
  conjunction as `Chabauty0ForcesCusp`. The scaffold cannot import
  `ColemanNonVanishing_26` (cycle through
  `J0_26_Q_RankZeroActual_26`).
* `X026RationalPointsActual_26` is unchanged (four-cusp audit).
* Typed `hGeomForbid` in `BealTheoremFromMazurChain26` stays the
  uninhabitable implication.

The v1.4.0 ledger SHA-256
`0259fe957cc348b7286e233ce717fac47c30ad174b05e8e1c5fb70626f511151`
is unchanged.

Chain: v4.1.1-descent-compute → this tag.

Version DOI: none (tag only).

Concept DOI: [10.5281/zenodo.22272382](https://doi.org/10.5281/zenodo.22272382)
