# v4.20.0 Iter Package None Docs Locked

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Locks [`docs/BealExponent13_Iter_Package.md`](../BealExponent13_Iter_Package.md)
to the v4.18.0 / v4.19.0 axiom-free table:

* `typed_and_forall` / `certified_typed_and_forall` none
* `verify-scaffold.sh` requires the file and greps
  `typed_and_forall`

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
No `sorry`. No `False.elim`.
