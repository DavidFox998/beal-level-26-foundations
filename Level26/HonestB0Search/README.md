# HonestB0Search (foundations v28, kolyvagin-fintype-subsingleton)

Lake targets `HonestB0Search` and `Level26` compile only this
folder. They do not enter `Level26/BealLevel26Foundations`
and do not join `.submodules Beal.Matveev`.

v28 Kolyvagin slice:
`Kolyvagin_Fintype_Subsingleton_inhabited.lean` —
`Nonempty (Fintype α) → Fintype α`, Subsingleton card `1` on
`Unit`, `|Sel₂|=1`, `3·7=21`, `L/Ω=1/3` and `1/7`,
`¬ IsRankZero` on `26a1`/`26b1`, from
`Kolyvagin_MW_Rank0_26a1_26b1_inhabited` (`6e88d94` /
`51bba93`).

`MW_rank_zero_fintype` stays `def Prop`. See
`docs/Kolyvagin_Fintype_Subsingleton_v28.md`.
