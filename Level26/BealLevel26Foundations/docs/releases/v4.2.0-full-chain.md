# v4.2.0-full-chain — exponent-13 full package (computational boundary)

GitHub release. Version DOI is recorded only after DataCite
`state: findable`. Concept DOI:
[10.5281/zenodo.22272382](https://doi.org/10.5281/zenodo.22272382).

This release names the exponent-13 Frey / `X₀(26)` chain as
computational packages. It does **not** add `sorry`, does **not**
inhabit typed `fourCusps → ¬ ExistsNoncuspidal`, and does **not**
prove `∀ A B C, ¬ A^13 + B^13 = C^13`.

Certified models (PARI 2.17.2; `Descent_26.json` SHA-256
`d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`):

| Curve | `[a₁,a₂,a₃,a₄,a₆]` | Δ | Conductor | Torsion | `ellrank` | `ell2cover` | `|Sel₂|` |
|---|---|---|---|---|---|---|---|
| 26a1 | `[1,0,1,-5,-8]` | `-17576` | 26 | 3 | `[0,0]` | empty | 1 |
| 26b1 | `[1,-1,1,-3,3]` | `-1664` | 26 | 7 | `[0,0]` | empty | 1 |

JSON `sha2_trivial: true` is computational, not a Mathlib Sha theorem.

* `SelmerBound_* = 1`; `rankZero_unconditional` is `1 = 1` twice.
* `certifiedM3_det_nonzero`: `det M₃ = 2 ≠ 0` over `ZMod 3`.
* `Chain/FreyCurve_13_26` — `FreyLevel26` is `2 * 13 = 26` (`rfl`).
  `FreyConductorDivides26` aliases that. `FreyToX0_26` is
  `26 = 2 * 13`. Not a Frey curve, not Ribet, not `True`/`trivial`.
  Mathlib 4.12 has no modularity and no Ribet.
* `Chain/RationalPoints_26_FourCusps_26` —
  `X0_26_Q_four` is `rankZero_unconditional ∧`
  `Chabauty0ForcesCusp_computational`.
  `X0_26_Q_eq_fourCusps` aliases that. Not a Mathlib `X₀(26)(ℚ)` theorem.
* `hGeomForbid_typed_computational` aliases `X0_26_Q_four`.
  Typed `hGeomForbid` stays uninhabitable
  (`hGeomForbid_typed_is_uninhabitable`).
* `BealTheorem_Exponent13_Full_package` is
  `X0_26_Q_four ∧ FreyLevel26`.
  `BealTheorem_Exponent13_Full` aliases that package.
  Not the Beal / Fermat statement for exponent 13.
* `X026RationalPointsActual_26` is unchanged.

The v1.4.0 ledger SHA-256
`0259fe957cc348b7286e233ce717fac47c30ad174b05e8e1c5fb70626f511151`
is unchanged.

Checks: `lake build` of the three Lake roots, `bash scripts/verify-scaffold.sh`,
and `python3 scripts/verify_descent_26.py`.

Chain: v4.1.3-beal-13-theorem → this tag.
