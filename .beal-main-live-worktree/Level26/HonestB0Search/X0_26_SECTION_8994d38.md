# `X₀(26)` at `8994d38` (`phase-darmon-merel-4413`)

This repo already has a **single** root `lakefile.lean`. The Lake
package is vendored at `Level26/BealLevel26Foundations` (beal-conjecture
@ `fea0c393` subdirectory only):

```
require beal_level_26_foundations from "Level26/BealLevel26Foundations"
```

Default `BealConjecture` wrappers and several Matveev modules resolve
through that local path. This tree is **not** a merge of
foundations-level-26 *into* beal-conjecture. Root
`TwoDescent_26a1_26.lean` (`7c19ad0`) and `BSD_MordellWeil.lean`
(`8994d38`) are the source of truth on
`BealMatveevBealV25B0Search`. The vendored
`lean/.../Descent/TwoDescent_26a1_26.lean` is an upstream snapshot.

Does **not** mint v25. No tag `v25-*`. `main` stays `6247c63`.
No `Towers/BSD/MordellWeil.lean` and no `BealTrueV25/` directory
(that path conflicts with root `BealTrueV25.lean`).

## González / LMFDB `26.42.2.a.1`

`Y² = x⁶ − 8x⁵ + 8x⁴ − 18x³ + 8x² − 8x + 1`, `f(0) = 1 ≠ 49`.
Affine theorems: LMFDB `(0,0)`, `(0,−1)` and González `(0,±1)`.
Genus numeral `1 + 42/12 − 2/4 − 0/3 − 4/2 = 2`. Completeness of
`X₀(26)(ℚ)` is Kenku / Chabauty and stays `def Prop`.

## Root `TwoDescent_26a1_26.lean` (`7c19ad0`)

- PARI `|Sel₂| = 1` (`Sel2_card_*` / `SelmerBound_*`)
- Sage `certified_mwrank` *display* `0`
- torsion-order Nats `3` and `7` (JSON `torsion: 2` is rejected)
- `2⁰ = 1`, `3 · 7 = 21`
- `Sel2_rank0_implies_rank0_*` is `1 = 1 → 0 = 0` on those Nats,
  not Mordell–Weil
- `RankZero_*_from_Selmer` / `Sha2_*_trivial` are `1 = 1`, not Sha[2]

## Root `BSD_MordellWeil.lean` (`8994d38`)

- `MordellWeilGroup` = Mathlib `WeierstrassCurve.toAffine.Point`
- `add_comm` from `AddCommGroup`; axioms `propext`,
  `Classical.choice`, `Quot.sound` (class-group proof, not
  “classical only”)
- Cremona `26a1` `[1,0,1,−5,−8]`, `Δ = −17576`, point `(4,4) ≠ 0`
- Cremona `26b1` `[1,−1,1,−3,3]`, `Δ = −1664`, point `(1,0) ≠ 0`
- `¬ IsRankZero` on both models: pasted `IsRankZero` is
  **Subsingleton** (only `{0}`), but torsion `ℤ/3ℤ` and `ℤ/7ℤ`
  are larger than `{0}`

**Uninhabited (`def Prop`):** `MW_rank_zero` (every point torsion);
`BSD_rank_statement` (analytic rank = algebraic rank);
`J0_26_rank0` stays on `X0_26_Full2Torsion`.

`J0_26_BSD_26a1_26b1` imports `BSD_MordellWeil`.

## Barrier lock (`953a174` → `8994d38`)

- `X₀(13)` genus 0: remaining cusps onto `𝔽₂₉ˣ`; Fricke scan
  cannot close Mazur (`1d28dc1`)
- `X₀(26)` González + `J0_26_rank0` `def Prop` + TwoDescent
  `7c19ad0` + BSD `8994d38`
- Tate `ℚ_[29]`: `v(Δ) = 26k`, truncated `j(q)` (`7244f6d`)
- Serre `|SL₂(𝔽₁₃)| = 2184`, `13 = 2²+3²`, `288/48 = 6`,
  `48 < 2184` (`e726c51`)
- Ribet displayed residual `928/29 = 32` (`a234427`)
- 143a1 template is rank-1, `ε = −1`, `L(E,1) = 0` — wrong sign
  for `26a1`/`26b1` (analytic rank 0, `L/Ω = 1/3`, `1/7`)
  (`e0b34b2`)

Builds: `lake build BealTrueV25`, `lake build BealMatveevBealV25B0Search`,
`./scripts/verify-matveev-beal.sh`. No new axiom, no Wiles, no
L-function.

Without-Wiles skeletons (still `def Prop`): see
[`roadmap_without_wiles/README.md`](roadmap_without_wiles/README.md).
