# v4.30.0 Iter Zenodo 22456594 Metadata Fix + Verify Grep

GitHub release. Updates `.zenodo.json` / `CITATION.cff` title to
name confirmed mints `22452680` → `22456594`. Locks those ids in
`verify-scaffold.sh` extra-loop and README greps.

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
`IsVersionOf` `10.5281/zenodo.22272382` is metadata only.
Original-concept latest remains v4.0.9 `10.5281/zenodo.22322627`.
