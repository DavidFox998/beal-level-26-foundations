# v4.0.14-coleman-integral-no-doi — named rank-zero / `det ≠ 0` Chabauty package

Git tag only. No Zenodo deposit and no version DOI.

Phase 3 Track C. Mathlib 4.12 has no `ℚ₂` Coleman integral and
no `X₀(26)(ℚ)` point type. This release does **not** add
`sorry`.

* `Jacobian/ColemanIntegral_26.lean` —
  `ColemanVanishesOnRankZero` is
  `J0_26_Q_RankZero26.of_qExpansion.rankZero`. Blueprint
  citation: Coleman 1985. The Lean `Prop` is the Sage
  rank-zero product. It is not a Coleman integral.
* `Jacobian/ColemanNonVanishing_26.lean` —
  `DifferentialNonZeroOnDisk` is `of_qExpansion.input.det ≠ 0`.
  `Chabauty0ForcesCusp` is those two packages conjoined. Not a
  Chabauty--Coleman theorem and not
  `∀ P : X₀(26)(ℚ)` in a residue disk.

`X026RationalPointsActual_26` is unchanged. Typed `hGeomForbid`
remains uninhabitable. Track D is not started. There is no
`theorem BealTheorem`.

Chain: v4.0.13-formal-immersion-proof-no-doi → this tag.

The v1.4.0 ledger SHA-256
`0259fe957cc348b7286e233ce717fac47c30ad174b05e8e1c5fb70626f511151`
is unchanged.

Version DOI: none (tag only).

Concept DOI: [10.5281/zenodo.22272382](https://doi.org/10.5281/zenodo.22272382)
