# v4.14.0 Iter Citation Contradiction Locked

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Locks `CITATION.cff` to the v4.13.0 computational boundary
at commit `c2c47db` and exports `certified_from_ribet` from
`Mazur/BealExponent13_Contradiction.lean` into the Iter
package.

* `CITATION.cff` version `v4.13.0-iter-contradiction-start`
  / commit `c2c47db`
* Abstract: X0(26) four cusps `[1,2,13,26]`, `|Sel₂|=1`
  twice, `det M₃=2`, SHA
  `d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`,
  Frey `2*13=26`, 26a1 `[1,0,1,-5,-8]` Δ `-17576`, 26b1
  `[1,-1,1,-3,3]` Δ `-1664`
* `certified_from_ribet` is the typed inhabitant
  `beal_exponent13_from_ribet` (`hGeomForbid_typed_true`
  `hNotIn hInList`, no axioms)
* `contradiction_from_ribet` remains
  `ExistsNoncuspidal_26 → False` via
  `ribet_level_lowering_26` only
* `-- SECURED BY certs/pari_x0_26_four_cusps.json +
  docs/images/2-descent-level-26-26a1-26b1.webp`
* `X0_26_Q = {P | P.label ∈ fourCuspsList}` `rfl`, no True
* `verify-scaffold.sh` unchanged: image ≥300 KB, Facebook
  name gone, README PARI path, JSON `26a1`/`26b1`/`SHA`,
  both contradiction theorems; no Zenodo record-id greps

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
Only axioms: `frey_modular_13` and `ribet_level_lowering_26`.
No `sorry`. No `False.elim`.
