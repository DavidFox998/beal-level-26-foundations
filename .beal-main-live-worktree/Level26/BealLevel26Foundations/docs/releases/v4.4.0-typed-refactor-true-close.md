# v4.4.0 Typed True Close (displayed cusp-label encoding)

GitHub release. Version DOI recorded only after DataCite
`state: findable`.

`ExistsNoncuspidal_26` is existence of a
`DisplayedX026CuspPoint` whose label is not on
`fourCuspsList = [1, 2, 13, 26]`.  Every such point already
carries `P.mem`, so the existential is empty by type.  The
trailing `True` is a placeholder for `P ∈ X0_26_Q`; Mathlib
4.12 has no `X0_26_Point`.

`hGeomForbid_typed_true` is
`fourCuspsForallCuspPoints → ¬ ExistsNoncuspidal_26`, proved
by `hNotIn hInList` without `False.elim`.  That is not the
old elliptic-`j` implication (`True → ¬True`, still
`#check hGeomForbid_typed_is_uninhabitable`) and not a
Mathlib `X₀(26)(ℚ)` theorem.

`BealTheorem_Exponent13_Typed` is that implication plus
`FreyLevel26` (`2 * 13 = 26`).
`BealExponent13_Final_Package` stays
`X0_26_Q_four ∧ FreyLevel26 ∧ fourCuspsForallCuspPoints`.

Certified: 26a1 `[1,0,1,-5,-8]` Δ `-17576` conductor 26 torsion 3;
26b1 `[1,-1,1,-3,3]` Δ `-1664` conductor 26 torsion 7.

PARI 2.17.2: `ellrank [0,0]`, empty `ell2cover`, `|Sel₂|=1` twice
(`1=1`), JSON Sha[2] computational, SHA-256
`d9d907f6cf29e9a90731184f082d430d33128f0f857e6a8124a1eef0b8e39260`.

Chabauty: `certifiedM3_det_nonzero` det=`2` over `ZMod 3`.
`X026RationalPointsActual_26` is unchanged.  No `sorry`.
No `∀ A B C, ¬ A^13 + B^13 = C^13`.

Releases: `v4.3.0-final-forall-package` `51663b6`, this tag.
