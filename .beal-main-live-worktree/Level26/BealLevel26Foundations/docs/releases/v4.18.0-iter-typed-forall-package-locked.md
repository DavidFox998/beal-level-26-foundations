# v4.18.0 Iter Typed Forall Package Locked

GitHub release. Release notes do not list Zenodo version
DOIs. A missing search hit is not a verify failure.

Locks `Typed ∧ Typed` via the axiom-free Forall bridge:

* `typed_and_forall` is
  `⟨beal_exponent13_from_ribet, beal_forall_from_ribet⟩`
* `certified_typed_and_forall` is
  `⟨certified_from_forall, beal_forall_certified_from_ribet⟩`
* Both `#print axioms` as none via
  `hGeomForbid_typed_true` (`hNotIn hInList`)
* `BealExponent13_Iter_Typed_And_Package` remains
  `Typed ∧ Iter_Package` (`rfl`)
* `verify-scaffold.sh` greps `typed_and_forall` and
  `certified_typed_and_forall`

Still not `∀ ℕ`. Still not a Mathlib `X₀(26)(ℚ)` theorem.
Upstream `BealExponent13_Contradiction.certified` still uses
both computational-assumption axioms. No `sorry`. No
`False.elim`.
